apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: development
spec:
  selector:
    app: web
  ports:
    - port: 80        # exposed by the service
      targetPort: 80  # pod's container port
  type: NodePort
